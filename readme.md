''' algorithm for the simple calculator
start
get user inputs 
condition:
    IF operator == :
        THEN: add num_1 , num_2
    else if operator == -:
        THEN: subtract num_1 , num_2
    else if operator == * :
        THEN: multiply num_1 ,num_2
    else if operarto == /:
        if 2nd number(num_2) == 0:
            THEN: print "error"
        else: 
            divide num_1 , num_2 ''' 